namespace Utils;

/// <summary>
/// Console colors for mapping.
/// </summary>
public enum Color
{
    Condition,
    Secondary,
    Primary,
    Error,
}

public enum ActionType
{
    Show,
    Sort,
    Update,
    Filter,
}

/// <summary>
/// Panel actions.
/// </summary>
public enum FieldType
{
    Id,
    Age,
    Name,
    Gender,
    Diagnosis,
    HeartRate,
    Temperature,
    OxygenSaturation,
    Doctors,
}